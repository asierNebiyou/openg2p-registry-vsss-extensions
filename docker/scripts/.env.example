# Docker Hub credentials — DO NOT COMMIT THIS FILE
# Copy this file to build/.env and fill in your values.
# build/.env is already listed in .gitignore.

# Required when using --push / PUSH=1
DOCKER_HUB_USERNAME=your_dockerhub_username
DOCKER_HUB_TOKEN=your_dockerhub_access_token

# When OpenG2P Hub repos are missing, retag/push under this namespace
# (e.g. asierneb/openg2p-vsss-staff-portal-api:develop).
# DOCKER_IMAGE_NAMESPACE=asierneb
# Also push these extra tags of the same image (space-separated).
# DOCKER_EXTRA_TAGS=1.1.2

# Optional overrides (uncomment to use)
# BUILD_PLATFORM=linux/amd64,linux/arm64
# NO_CACHE=1
