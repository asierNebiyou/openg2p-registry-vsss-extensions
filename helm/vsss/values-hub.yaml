# Overlay for Hub tags that exist today.
#
# Staff / partner / celery: OpenG2P platform images + VSSS extension via
# REGISTRY_EXTENSION_MODULE (see values.yaml global.registryExtension*).
# db-seed and connector still have no OpenG2P Hub repos.

dbSeed:
  image:
    repository: asierneb/openg2p-vsss-db-seed
    tag: develop
    PullSecrets: ""

staffPortalUi:
  image:
    repository: openg2p/openg2p-registry-staff-portal-ui
    tag: "1.1.1"

connector:
  image:
    repository: asierneb/openg2p-connector-service
    tag: nsr-slashfix-202605221435
    PullSecrets: ""
  ui:
    image:
      repository: asierneb/openg2p-connector-ui
      tag: nsr
